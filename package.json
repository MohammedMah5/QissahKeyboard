{
  "name": "qissah-keyboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "firebase deploy",
    "lint": "echo 'No linter configured'",
    "format": "echo 'No formatter configured'"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
